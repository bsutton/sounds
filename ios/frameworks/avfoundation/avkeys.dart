///These are all defined as NSString *const AVEncoderBitRateKey;
///That being said based on their usage is think a generic enum is
///how they should be mapping in dart.

enum AVKeys {
  AVSampleRateKey,
  AVFormatIDKey,
  AVNumberOfChannelsKey,
  AVEncoderAudioQualityKey,
  AVEncoderBitRateKey
}
